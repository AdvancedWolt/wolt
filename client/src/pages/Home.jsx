// EX4-5 (P2 / Itay): main screen — restaurant feed + promoted/category rows.
// Skeleton stub. Fetch with getRestaurants() into state and render
// RestaurantCard components; handle loading / empty / error states.
const Home = () => (
    <section>
        <h1>Home</h1>
        <p>Restaurant feed goes here (EX4-5).</p>
    </section>
);

export default Home;
