// EX4-3 (P1 / Elad): login screen.
// Skeleton stub. Build the form here: validate inputs, call useAuth().login(),
// show a clear error on bad credentials, then navigate to "/".
const Login = () => (
    <section className="page">
        <h1>Log in</h1>
        <p>Login form goes here (EX4-3).</p>
    </section>
);

export default Login;
