// EX4-8 / EX4-9 (P3 / Dorian): cart checkout and order history.
// Skeleton stub (protected route). Fetch with getOrders(), show status, allow
// cancelOrder() on pending orders; checkout uses createOrder(restaurantId, items).
const Orders = () => (
    <section>
        <h1>Orders</h1>
        <p>Order history goes here (EX4-8 / EX4-9).</p>
    </section>
);

export default Orders;
